name: Issue Notification
on:
  issues:
    types: [opened, edited, closed, reopened]
  issue_comment:
    types: [created]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Google Chat Notification
        # ツールを使わず、直接Google ChatのWebhookを叩くもっともシンプルな方法に変えます
        run: |
          curl -X POST "${{ secrets.GOOGLE_CHAT_WEBHOOK }}" \
          -H "Content-Type: application/json; charset=UTF-8" \
          -d '{
            "text": "📋 *Issue更新: ${{ github.event.issue.title }}*\n\n👤 *実行者*: ${{ github.actor }} (${{ github.event.action }})\n\n📝 *内容*:\n${{ github.event.issue.body || github.event.comment.body }}\n\n🔗 *URL*: ${{ github.event.issue.html_url }}"
          }'
